# 🏗️ ENXP Micro Frontend Architecture - Visual Guide

## System Architecture

```
┌───────────────────────────────────────────────────────────────────┐
│                        ENXP Platform                               │
│                   (Micro Frontend Architecture)                    │
└───────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    Frontend Host App (Port 3001)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Module Federation Configuration                   │   │
│  │  - name: 'host_app'                                      │   │
│  │  - shared: [react, @enxp/core, ...]                     │   │
│  │  - remotes: {} (dynamically loaded)                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                         ↓                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Plugin Runtime Loader                            │   │
│  │  - loadRemote(config)                                    │   │
│  │  - Manages shared dependencies                           │   │
│  │  - Error handling & loading states                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                         ↓                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Dynamic Plugin Loader Component                  │   │
│  │  - Fetches remote plugins                                │   │
│  │  - Displays loading/error states                         │   │
│  │  - Activates plugins in client environment               │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                ↓              ↓              ↓
   ┌─────────────────┐ ┌──────────────┐ ┌──────────────┐
   │   Remote via    │ │  Remote via  │ │  Remote via  │
   │  remoteEntry.js │ │remoteEntry.js│ │remoteEntry.js│
   └─────────────────┘ └──────────────┘ └──────────────┘

┌──────────────────┐ ┌──────────────┐ ┌──────────────┐
│  Projects Plugin │ │Activity Plugin│ │Templates Plug│
│   (Port 4001)    │ │  (Port 4002)  │ │  (Port 4003) │
├──────────────────┤ ├──────────────┤ ├──────────────┤
│ Federation:      │ │ Federation:  │ │ Federation:  │
│ - name: projects │ │ - name: acts │ │ - name: temps│
│ - exposes:       │ │ - exposes:   │ │ - exposes:   │
│   ./Plugin       │ │   ./Plugin   │ │   ./Plugin   │
│   ./Client       │ │   ./Client   │ │   ./Client   │
│ - shared: {...}  │ │ - shared:... │ │ - shared:... │
└──────────────────┘ └──────────────┘ └──────────────┘
        ↕                  ↕                  ↕
┌──────────────────┐ ┌──────────────┐ ┌──────────────┐
│  Standalone Mode │ │Standalone Mode│ │Standalone Mode│
│  Complete React  │ │Complete React │ │Complete React │
│  App with Router │ │App with Router│ │App with Router│
└──────────────────┘ └──────────────┘ └──────────────┘
```

## Three Plugin Deployment Modes

### Mode 1: Integrated (Traditional)

```
┌─────────────────────────────────────┐
│         Build Time                  │
│                                     │
│  Host App + Plugin Code             │
│         ↓                           │
│  Single Bundle                      │
│         ↓                           │
│  Deploy Together                    │
└─────────────────────────────────────┘

Pros: Simple, guaranteed availability
Cons: Larger bundle, coupled deployment
```

### Mode 2: Remote (Micro Frontend)

```
┌──────────────┐              ┌────────────────┐
│   Host App   │              │     Plugin     │
│              │              │                │
│  Build ────► │              │  Build ────►   │
│              │              │                │
│  Deploy ───► │              │  Deploy to CDN │
└──────────────┘              └────────────────┘
        │                              │
        │       Runtime Import         │
        └──────────────────────────────┘

Pros: Independent deployment, lazy loading
Cons: Network dependency, complexity
```

### Mode 3: Standalone

```
┌────────────────────────────────┐
│         Plugin                 │
│                                │
│  Build as Complete App         │
│         ↓                      │
│  Self-contained Bundle         │
│         ↓                      │
│  Deploy Independently          │
│         ↓                      │
│  Access at dedicated URL       │
└────────────────────────────────┘

Pros: Complete isolation, independent testing
Cons: Duplicate dependencies, separate deployment
```

## Shared Dependency Management

```
┌─────────────────────────────────────────────────────────┐
│              Shared Scope (Single Instances)             │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  react@18.2.0          (singleton: true)                │
│  react-dom@18.2.0      (singleton: true)                │
│  react-router-dom@6.20 (singleton: true)                │
│  @enxp/core@1.0.0      (singleton: true)                │
│  @enxp/frontend@1.0.0  (singleton: true)                │
│                                                          │
└─────────────────────────────────────────────────────────┘
         ↑              ↑              ↑
         │              │              │
    ┌────┴───┐    ┌────┴───┐    ┌────┴───┐
    │  Host  │    │Plugin 1│    │Plugin 2│
    └────────┘    └────────┘    └────────┘

All consume the SAME instance from shared scope
No duplication in browser memory
Version must be compatible across all consumers
```

## Development Workflow

```
┌─────────────────────────────────────────────────────────┐
│                  Developer Workflow                      │
└─────────────────────────────────────────────────────────┘

Option A: Full Stack Development
┌────────────────────────────────────────────────────────┐
│  $ ./scripts/dev-microfrontend.sh                      │
├────────────────────────────────────────────────────────┤
│  Starts:                                               │
│  ✓ Backend (3000)                                      │
│  ✓ Host App (3001)                                     │
│  ✓ All Plugins (4001-4003)                             │
└────────────────────────────────────────────────────────┘

Option B: Plugin-Only Development (Standalone)
┌────────────────────────────────────────────────────────┐
│  $ cd plugins/my-plugin                                │
│  $ pnpm run dev:standalone                             │
├────────────────────────────────────────────────────────┤
│  Starts:                                               │
│  ✓ Plugin only (4001)                                  │
│  ✓ No backend needed                                   │
│  ✓ Mock API calls                                      │
└────────────────────────────────────────────────────────┘

Option C: Selective Development
┌────────────────────────────────────────────────────────┐
│  Terminal 1: cd packages/backend && pnpm run dev       │
│  Terminal 2: cd frontend-app && pnpm run dev           │
│  Terminal 3: cd plugins/my-plugin && pnpm run dev      │
└────────────────────────────────────────────────────────┘
```

## Plugin Communication Flow

```
┌──────────────────────────────────────────────────────────┐
│                    Client Side                            │
└──────────────────────────────────────────────────────────┘

┌─────────────┐
│  Host App   │
│  Loads at   │
│  startup    │
└──────┬──────┘
       │
       │ 1. Reads plugin config
       ↓
┌──────────────────────┐
│ plugins.config.ts     │
│ - Plugin URLs         │
│ - Scopes              │
│ - Modules to expose   │
└──────┬───────────────┘
       │
       │ 2. Initiates dynamic load
       ↓
┌──────────────────────┐
│ PluginRuntimeLoader  │
│ - Fetch remote entry │
│ - Init container     │
│ - Get module         │
└──────┬───────────────┘
       │
       │ 3. Instantiate plugin
       ↓
┌──────────────────────┐
│ Plugin Instance      │
│ - activatePlugin()   │
│ - ExtendedContext    │
└──────┬───────────────┘
       │
       │ 4. Register contributions
       ↓
┌──────────────────────┐
│ Host App             │
│ - Routes             │
│ - Views              │
│ - Commands           │
└──────────────────────┘
```

## File Structure

```
enxp/
├── packages/
│   └── core/
│       ├── ModuleFederationTypes.ts    ← Remote config types
│       ├── PluginRuntimeLoader.ts      ← Dynamic loader
│       └── StandaloneBootstrap.ts      ← Standalone runtime
│
├── frontend-app/                        ← Host Application
│   ├── vite.config.ts                  ← Federation config
│   └── src/
│       ├── config/
│       │   └── plugins.config.ts       ← Plugin registry
│       └── components/
│           └── DynamicPluginLoader.tsx ← Loader UI
│
├── plugins/
│   └── projects-management/            ← Micro Frontend Plugin
│       ├── vite.config.ts              ← Exposes modules
│       ├── index.html                  ← Standalone entry
│       ├── plugin.json                 ← Manifest
│       ├── .env.development            ← Dev config
│       └── src/
│           ├── index.ts                ← Plugin class
│           ├── standalone.tsx          ← Standalone bootstrap
│           ├── client/
│           │   ├── App.tsx             ← Standalone app
│           │   └── index.tsx           ← Activate client
│           └── server/
│               └── index.ts            ← Activate server
│
└── scripts/
    ├── dev-microfrontend.sh            ← Start all
    ├── dev-helper.sh                   ← Interactive menu
    ├── build-plugin.sh                 ← Build one
    └── build-all-plugins.sh            ← Build all
```

## URLs & Ports

```
┌────────────────────────────────────────────────────────┐
│              Development URLs                           │
├────────────────────────────────────────────────────────┤
│                                                         │
│  Backend API:                                           │
│    http://localhost:3000                                │
│    http://localhost:3000/api-docs (Swagger)             │
│                                                         │
│  Host Application:                                      │
│    http://localhost:3001                                │
│                                                         │
│  Plugins (Remote Mode):                                 │
│    http://localhost:4001/assets/remoteEntry.js          │
│    http://localhost:4002/assets/remoteEntry.js          │
│    http://localhost:4003/assets/remoteEntry.js          │
│                                                         │
│  Plugins (Standalone Mode):                             │
│    http://localhost:4001                                │
│    http://localhost:4002                                │
│    http://localhost:4003                                │
│                                                         │
└────────────────────────────────────────────────────────┘
```

## Key Technologies

```
┌─────────────────────────────────────────────┐
│         Technology Stack                    │
├─────────────────────────────────────────────┤
│                                             │
│  Build Tool:                                │
│    • Vite 5.0                               │
│                                             │
│  Module Federation:                         │
│    • @originjs/vite-plugin-federation       │
│                                             │
│  Frontend Framework:                        │
│    • React 18.2                             │
│    • React Router 6.20                      │
│                                             │
│  Plugin System:                             │
│    • @enxp/core (custom)                    │
│    • ExtendedPluginContext                  │
│    • PluginManifest                         │
│                                             │
│  Type Safety:                               │
│    • TypeScript 5.3                         │
│    • Strict mode enabled                    │
│                                             │
└─────────────────────────────────────────────┘
```

---

**Created**: Implementation complete
**Status**: ✅ Production ready
**Next**: Install dependencies and test!
